# MealMuse - 技术选型与架构文档

> 版本：v7.0（技术栈：Taro 4.x + 小程序 + H5）\
> 更新日期：2026-05-11\
> 开发周期：1周 MVP\
> 产品载体：微信小程序（核心），H5 演示，预留 App 扩展

***

## ⚠️ 安全红线（置顶必读）

### 1. API Key 绝不暴露到前端

| 类型       | 存储位置                         | 示例                                   |
| -------- | ---------------------------- | ------------------------------------ |
| ✅ 后端环境变量 | `apps/server/.env`（永不提交 Git） | `LLM_API_KEY`、`MONGODB_URI`          |
| ❌ 前端代码   | 任何地方都不行                      | `const API_KEY = 'sk' + '-' + 'xxx'` |
| ❌ 前端环境变量 | `.env.local` 也不行             | 前端打包后所有变量都暴露                         |

**为什么前端** **`.env.local`** **也不安全？**

- Taro/Vite 的 `process.env` 变量会在打包时注入到代码中
- 小程序/H5 代码可以被反编译或查看源码
- 永远不要在前端代码中硬编码 API Key

### 2. 环境变量管理规范

**后端（安全）**：

```bash
# apps/server/.env
LLM_API_KEY=YOUR_LLM_API_KEY
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net

# .gitignore 必须包含
.env
.env.local
.env.*.local
```

**前端（只能放公开配置）**：

```bash
# apps/client/.env
# 只能放：接口地址、版本号、开关配置
VITE_API_BASE_URL=https://api.mealmuse.com
VITE_APP_VERSION=1.0.0
```

### 3. 代码审查红线

提交 PR 前必须检查：

- [ ] 没有任何形式的密钥/Token/密码硬编码（示例：`sk`+`-` 前缀类 Token、JWT、数据库密码等）
- [ ] 没有 `api.openai.com` 或 `api.deepseek.com` 直接出现在前端
- [ ] `.env` 文件在 `.gitignore` 中
- [ ] 没有 `console.log(process.env)` 调试代码残留

### 4. 泄露了怎么办？

**立刻**：

1. 撤销该 API Key（DeepSeek/通义/OpenAI 后台操作）
2. 生成新 Key，更新后端 `.env`
3. 检查账单，确认没有异常调用

***

## 〇、目标与范围

### P0 / P1 定义（统一口径）

- **P0**：1 周 MVP 必须交付，且以**微信小程序可用**为交付口径；H5 允许作为演示/联调载体，但不改变“小程序为核心交付”的前提。
- **P1**：可上线试运行阶段增强项；不影响 MVP 主流程闭环，按迭代资源择机推进。

### 本期目标（P0/P1）

| 功能      | 优先级 | 说明                              |
| ------- | --- | ------------------------------- |
| 单人吃饭决策  | P0  | 3问极简筛选 → 1主推+2备选+1反推，支持"按方向换一个" |
| 多人饭局决策  | P0  | 建饭局房间 → 偏好录入 → 群体折中推荐+满意度解释     |
| AI 推荐解释 | P0  | 结构化预过滤选什么 + 大模型解释为什么            |
| 会话历史记录  | P0  | 先 localStorage，后端允许时同步 DB       |
| 历史偏好记忆  | P1  | 轻量画像（常选/拒绝/最近吃过）→ 影响重排序与默认值     |

### 非目标（本期不做）

- 复杂的机器学习训练/召回体系（离线训练、向量检索等）
- 实时爬取美团/饿了么（后续数据源扩展）
- 复杂账号权限体系（先匿名 ID，后接登录）
- App 原生功能（推送、相机等，后续考虑）

***

## 一、技术栈总览

```
┌─────────────────────────────────────────────────────────────────┐
│                    pnpm Monorepo (workspaces)                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  packages/shared/  (前后端共享类型/常量/Prompt模板)         │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────┐  ┌────────────────────────────────┐  │
│  │  apps/client/        │  │  apps/server/                  │  │
│  │  Taro 4.x 前端       │  │  NestJS 后端                   │  │
│  │  ┌──────────────┐    │  │  ┌──────────────────────────┐  │  │
│  │  │ 微信小程序   │    │  │  │ @nestjs/websockets       │  │  │
│  │  │ H5 演示      │    │  │  │ @nestjs/mongoose          │  │  │
│  │  └──────────────┘    │  │  │ LLM Service (OpenAI SDK) │  │  │
│  │  React 18 + TS       │  │  └──────────────────────────┘  │  │
│  │  Zustand              │  │  Socket.IO 4.x                │  │
│  └──────────────────────┘  └────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

***

## 二、前端技术选型

### 2.1 核心框架：Taro 4.x + React 18

| 选型             | 版本    | 选型理由                               |
| -------------- | ----- | ---------------------------------- |
| **Taro**       | 4.1.x | 一套 React 心智覆盖 H5/小程序，适合 MVP 快速验证   |
| **React**      | 18.x  | Taro 官方主推，Hooks 生态成熟，团队熟悉度高        |
| **TypeScript** | 5.x   | 前后端统一类型体系，接口 DTO/枚举/校验规则可共享，减少联调成本 |

**平台优先级（统一口径）**：

1. **交付优先级（P0）**：微信小程序（核心交付）
2. **开发/演示优先级（P0）**：H5（开发调试最快、演示方便，不承诺对外长期运营）
3. **扩展优先级（P1）**：App（Taro-RN 坑位较多，后续评估）

### 2.2 状态管理：Zustand

| 方案对比 | Zustand        | Redux  | MobX   |
| ---- | -------------- | ------ | ------ |
| 学习成本 | ⭐ 极低           | 高      | 中      |
| 样板代码 | ⭐ 无            | 多      | 少      |
| 跨端支持 | H5/小程序完美，RN需验证 | ⭐ 官方支持 | 官方支持   |
| 包体积  | ⭐ \~1KB        | \~10KB | \~15KB |

**选型理由**：MVP 阶段追求极致开发效率，Zustand 零样板代码，无需 Provider 包裹。

### 2.3 UI 组件库：antd-mobile（H5）+ Taro UI（小程序条件编译）

| 方案对比  | antd-mobile   | Taro UI      | NutUI        |
| ----- | ------------- | ------------ | ------------ |
| 你熟悉度  | ⭐ 熟悉          | 一般           | 不熟悉          |
| 维护状态  | ⭐ 蚂蚁持续维护，周更   | 社区活跃度一般，更新慢  | 京东维护，稳定      |
| 组件质量  | ⭐ 企业级，60+ 组件  | 基础够用，部分有 bug | 丰富，Taro 生态最好 |
| H5 体验 | ⭐ 原生支持，体验最好   | 需额外处理兼容      | 好            |
| 小程序支持 | ⚠️ 不直接支持 Taro | ⭐ 原生支持       | ⭐ 原生支持       |

**选型方案**：

- **H5 端（P0 / 演示+联调）**：antd-mobile，你熟悉，开发最快，体验最好
- **小程序端（P0 / 核心交付）**：Taro UI（必要组件按需引入），通过 Taro 条件编译按端引入不同组件
- **后续统一（P1）**：如小程序端体验与维护成本成为主要矛盾，再评估迁移 NutUI（不作为 MVP 前置）

**条件编译示例**：

```typescript
// 按端引入不同组件
import { Button } from 'antd-mobile';  // H5 默认

// #ifdef WEAPP
import { AtButton } from 'taro-ui';     // 小程序端降级
// #endif
```

**MVP 阶段策略（统一口径）**：小程序主流程必须闭环；H5 作为开发调试与演示载体优先迭代。Taro 的核心价值不是“同时做多端”，而是“同一套代码在不同端可控落地”，避免未来重写。

### 2.4 样式方案

| 决策项      | 方案                              | 理由                                                             |
| -------- | ------------------------------- | -------------------------------------------------------------- |
| CSS 预处理器 | **Less**                        | 你熟悉，Taro 官方支持                                                  |
| 设计变量管理   | **shared 包 tokens.less**        | 跨端共享颜色/间距/圆角等变量                                                |
| 组件库主题    | **CSS 变量桥接**                    | tokens → antd-mobile 变量 / Taro UI 变量，一处改全局生效                   |
| 业务样式     | **CSS Modules**（`.module.less`） | Taro 原生支持，自动局部作用域，不用手动起 BEM 名                                  |
| 跨端单位     | **Taro 自动转换**                   | `designWidth: 750`，代码写 px，编译时自动转 rpx                           |
| 暗黑模式     | **预留 CSS 变量**                   | `[data-theme='dark']` 覆盖 tokens，MVP 不实现切换                      |
| 图标       | **@ant-design/icons**           | H5 用 `@ant-design/icons`，小程序用 `@ant-design/icons-react`，条件编译切换 |
| 安全区域     | **env() mixin**                 | `constant/env(safe-area-inset-bottom)` 适配 iPhone 横条            |

**样式分层架构**：

```
Layer 0: Design Tokens（shared 包，颜色/间距/圆角/字体变量）
Layer 1: Base Styles（全局 reset、字体、安全区域）
Layer 2: Component Override（antd-mobile / Taro UI 主题变量覆盖）
Layer 3: Business Styles（页面/组件 .module.less）
```

**主题统一原理**：antd-mobile 5.x 基于 CSS 变量，将 `--adm-color-primary` 等变量指向我们的 Design Tokens，实现一处定义、全局统一。

### 2.5 网络请求：Taro.request

- **统一网络形态**：HTTP（业务请求） + Socket.IO（实时/流式）
- **HTTP**：用于推荐请求、历史/画像等普通接口（请求-响应）
- **Socket.IO**：用于多人房间实时同步；同时作为 LLM 文案**流式下发通道（可选）**
- **小程序限制（明确声明）**：
  - 不依赖浏览器侧 `ReadableStream`/SSE 能力（小程序环境支持不一致、可维护性差）
  - 若尝试 `enableChunked`，必须准备“非流式一次性返回”的降级路径；正式版默认以 Socket.IO 做流式，不把 chunked 作为主路径

***

## 三、后端技术选型

### 3.1 Web 框架：NestJS 11.x

| 框架对比          | NestJS                          | Express      | Fastify |
| ------------- | ------------------------------- | ------------ | ------- |
| 代码规范性         | ⭐ 框架强制（模块/装饰器/DI）               | 依赖自觉         | 依赖自觉    |
| TypeScript 支持 | ⭐ 一等公民                          | 需手动配置        | 内置      |
| Socket.IO 集成  | ⭐ `@nestjs/websockets` 官方包      | 手动配          | 手动配     |
| MongoDB 集成    | ⭐ `@nestjs/mongoose` 官方包        | 手动配 Mongoose | 手动配     |
| Monorepo 支持   | ⭐ 内置（`@nestjs/cli` monorepo 模式） | 无            | 无       |
| 学习成本          | 中（需学装饰器/DI/模块）                  | ⭐ 最低         | 低       |
| 1周MVP适配度      | ★★★★☆                           | ★★★★★        | ★★★★☆   |

**选型理由**：

- **Monorepo 天然支持**：NestJS CLI 内置 monorepo 模式，与 pnpm workspaces 完美配合
- **官方包覆盖核心需求**：`@nestjs/websockets`（Socket.IO）、`@nestjs/mongoose`（MongoDB）开箱即用
- **强制规范**：模块化、依赖注入、装饰器路由，代码结构清晰，路演时展示工程能力
- **前后端类型共享**：NestJS 是纯 TypeScript，与 Taro 前端共享 DTO/枚举零成本
- **代价**：Day 1 多花 2-3 小时搭项目结构（学习装饰器和 DI 基础）

### 3.2 数据库：MongoDB + Mongoose 8.x（@nestjs/mongoose）

| 方案对比       | MongoDB + Mongoose       | PostgreSQL        | SQLite |
| ---------- | ------------------------ | ----------------- | ------ |
| Schema 灵活性 | ⭐ 极高                     | 中                 | 低      |
| NestJS 集成  | ⭐ `@nestjs/mongoose` 官方包 | `@nestjs/typeorm` | 无官方包   |
| 开发效率       | ⭐ 高                      | 中                 | 高      |
| 部署复杂度      | 低（Atlas 云服务）             | 中                 | ⭐ 极低   |

**选型理由**：

- 用户偏好、历史记录都是 JSON 结构，MongoDB 天然适配
- `@nestjs/mongoose` 提供模块化 Schema 定义，与 NestJS DI 系统无缝集成
- MongoDB Atlas 免费层足够 MVP 使用，零运维成本
- **不需要向量数据库**：餐饮数据量小，不需要检索系统；线上推荐仍按规则/打分先收敛到固定 20 条候选，再交给 LLM（详见 4.7 节）

### 3.3 实时通信：Socket.IO 4.x（@nestjs/websockets）

| 方案对比       | @nestjs/websockets      | 手动集成 Socket.IO |
| ---------- | ----------------------- | -------------- |
| NestJS 集成  | ⭐ 装饰器声明式，官方维护           | 手动挂载到 HTTP 服务器 |
| Gateway 模块 | ⭐ 内置（WebSocket Gateway） | 需自行封装          |
| 房间机制       | ⭐ 原生 Room + 装饰器         | 原生 Room        |
| 自动重连/降级    | ⭐ 内置                    | 内置             |

**选型理由**：

- `@nestjs/websockets` + `@nestjs/platform-socket.io` 提供声明式 Gateway，用装饰器定义事件处理
- 与 NestJS 模块系统、DI、Guard、Pipe 等基础设施无缝集成
- 房间机制原生支持，多人房间场景完美匹配

### 3.4 LLM 调用：OpenAI SDK（统一接口，封装为 NestJS Service）

**核心发现：DeepSeek、通义千问均兼容 OpenAI API 格式，一行 baseURL 切换模型。**

```javascript
const OpenAI = require('openai');

// 通过环境变量切换，代码无需改动
const client = new OpenAI({
  apiKey: process.env.LLM_API_KEY,
  baseURL: process.env.LLM_BASE_URL  // 不同模型只改这里
});

// 调用方式完全一致
const response = await client.chat.completions.create({
  model: process.env.LLM_MODEL,
  messages: [{ role: "user", content: "..." }]
});
```

**选型理由**：

- 一行 `baseURL` 切换 DeepSeek/通义/OpenAI，无需多套 SDK
- 原生支持流式响应 (streaming)
- 提示词模板可版本化管理，输出结构约束为 JSON 便于前端展示与后端存储

***

## 四、推荐系统架构设计

### 4.1 核心设计理念

> **"结构化推荐引擎选什么 + 大模型解释为什么"**
>
> 不做"纯大模型随便推荐"（不可控），也不做"纯公式打分"（无算法能力）。
> 采用中间路线：轻量规则预过滤 + Prompt 驱动 AI 决策 + AI 生成自然语言解释。

### 4.2 NestJS 后端模块划分

```
src/
├── app.module.ts                    # 根模块
├── meal/                            # 餐食库模块
│   ├── meal.module.ts
│   ├── meal.controller.ts           # RESTful API
│   ├── meal.service.ts              # CRUD + 筛选逻辑
│   ├── meal.schema.ts               # Mongoose Schema
│   └── dto/                         # 请求/响应 DTO
├── recommend/                       # 推荐引擎模块（核心）
│   ├── recommend.module.ts
│   ├── recommend.controller.ts
│   ├── recommend.service.ts         # 推荐 Pipeline 编排
│   ├── rule-filter.service.ts       # 硬约束过滤
│   ├── scoring.service.ts           # 场景化打分
│   ├── rerank.service.ts            # 重排序
│   └── group-recommender.service.ts # 多人群体推荐
├── llm/                             # 大模型模块
│   ├── llm.module.ts
│   ├── llm.service.ts               # OpenAI SDK 封装
│   ├── prompts/                     # Prompt 模板（从 shared 包引用）
│   └── llm-fallback.service.ts      # 降级策略
├── room/                            # 多人房间模块
│   ├── room.module.ts
│   ├── room.gateway.ts              # WebSocket Gateway（Socket.IO）
│   ├── room.controller.ts           # HTTP API（创建/查询房间）
│   ├── room.service.ts              # 房间生命周期管理
│   └── room.schema.ts
├── session/                         # 会话与历史模块
│   ├── session.module.ts
│   ├── session.controller.ts
│   ├── session.service.ts
│   └── session.schema.ts
└── profile/                         # 用户偏好画像模块
    ├── profile.module.ts
    ├── profile.service.ts
    └── profile.schema.ts
```

### 4.3 四层推荐流程（单人）

```
┌─────────────────────────────────────────────────────────────┐
│  第0层：硬约束过滤 (RuleFilter, JS filter)                    │
│  ├─ 忌口命中 → 直接排除                                      │
│  ├─ 辣度不匹配 → 排除或大扣分                                 │
│  ├─ 时间超限 → 直接排除                                      │
│  ├─ 预算超限 → 直接排除；"稍超"保留但扣分                     │
│  └─ 输入：全量数据 (100条) → 输出：候选池 (80条)              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  第1层：候选集筛选 (Scoring, JS filter)                       │
│  ├─ 预算过滤 (±5元浮动)                                      │
│  ├─ 时间过滤 (±10分钟浮动)                                   │
│  └─ 输入：80条 → 输出：候选池 (20条)                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  第2层：AI 决策 (LLM API, 核心)                              │
│  ├─ Prompt 包含：候选池（固定 20 条）+ 用户偏好 + 场景权重规则 │
│  ├─ AI 输出：1主推 + 2备选 + 1反推 + 匹配度分数               │
│  └─ 输入：20条候选 + Prompt → 输出：结构化推荐结果            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  第3层：AI 解释 (LLMExplanation, 同一次调用)                  │
│  ├─ 生成推荐理由 (3条)                                       │
│  ├─ 生成小缺点 (1条)                                         │
│  ├─ 生成反推荐理由 (1条)                                     │
│  └─ 输入：推荐结果 → 输出：自然语言解释                       │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 场景化权重规则

根据第 1 问"最在意什么"，动态切换推荐权重。这是本项目的核心技术点之一。

| 场景         | 时间  | 预算  | 口味  | 健康  | 饱腹/性价比       |
| ---------- | --- | --- | --- | --- | ------------ |
| **赶时间**    | 40% | 15% | 15% | 5%  | 距离25%        |
| **省钱**     | 10% | 40% | 10% | 5%  | 性价比25%+饱腹20% |
| **吃爽**     | 5%  | 10% | 40% | 5%  | 舒适感25%+情绪15% |
| **健康一点**   | 20% | 15% | 15% | 40% | 饱腹10%        |
| **不想动别踩雷** | 15% | 20% | 15% | 15% | 稳定35%        |

**实现方式**：将权重规则写进 Prompt，由 AI 理解并执行打分决策。你们不需要自己写打分算法。

### 4.5 Prompt 三段式组织

建议把 Prompt 拆成三段，便于管理和迭代：

```
┌─────────────────────────────────────────────┐
│  第一段：System（角色与输出约束）              │
│  ├─ 角色定义：校园吃饭决策助手                │
│  ├─ 风格要求：简洁、有说服力、避免胡编        │
│  └─ 输出格式：JSON 结构约束                  │
├─────────────────────────────────────────────┤
│  第二段：UserContext（用户偏好）               │
│  ├─ 场景/预算/口味/忌口等结构化字段           │
│  └─ 历史偏好画像（常选类型/最近吃过）         │
├─────────────────────────────────────────────┤
│  第三段：RecommendationContext（候选信息）     │
│  ├─ 候选餐厅列表（已过滤）                   │
│  ├─ 场景权重规则                            │
│  └─ 多样性约束（备选需与主推差异）           │
└─────────────────────────────────────────────┘
```

**Prompt 示例**：

```
【System】
你是校园吃饭决策助手。请根据用户偏好和候选餐厅，做出推荐决策。
输出必须为 JSON 格式，包含 main、alternatives(2条)、anti 三个字段。
推荐理由要简洁、自然、有说服力，不要胡编数据。

【UserContext】
场景：赶时间（权重：时间40% + 距离25% + 预算15% + 口味15% + 健康5%）
预算：20元以内
想吃：米饭类
忌口：香菜
历史偏好：常选米饭类，最近吃过鸡腿饭（同类扣分）

【RecommendationContext】
候选餐厅（已过滤忌口和超预算项）：
1. 番茄牛腩饭 - 18元 - 22分钟 - 标签：热乎、下饭、赶课友好
2. 鸡腿饭 - 15元 - 18分钟 - 标签：快餐、饱腹
3. 砂锅米线 - 16元 - 25分钟 - 标签：热汤、暖胃
...（共20条）

多样性约束：备选需与主推在类别/口味/形式上保持差异。
反推荐需选择"看起来可能会选、但因场景/限制不合适"的项。

请输出：
{
  "oneLiner": "一句话推荐",
  "main": { "id": "", "name": "", "matchScore": 87, "price": 18, "waitTime": 22, "reasons": ["理由1", "理由2", "理由3"], "downside": "小缺点1条" },
  "alternatives": [
    { "id": "", "name": "", "matchScore": 75, "price": 15, "waitTime": 18, "reasons": ["理由1", "理由2", "理由3"] },
    { "id": "", "name": "", "matchScore": 70, "price": 16, "waitTime": 25, "reasons": ["理由1", "理由2", "理由3"] }
  ],
  "anti": { "name": "麻辣香锅", "reasons": ["原因1", "原因2"] },
  "switchHints": { "cheap": "鸡腿饭", "fast": "鸡腿饭", "healthy": "砂锅米线" }
}
```

### 4.6 LLM 降级策略

避免模型出问题导致功能不可用：

```
LLM 调用
  │
  ├─ 成功且输出合规 → 返回 AI 生成结果
  │
  ├─ 输出不合规（JSON 解析失败）
  │   └─ Schema 校验不合格 → 重试 1 次
  │       └─ 仍失败 → 降级到模板化理由
  │
  ├─ 模型超时/报错
  │   └─ 降级到模板化理由（用后端得分原因拼装）
  │
  └─ 模板化理由示例：
      "这道番茄牛腩饭在你的预算内，出餐速度较快，适合赶时间。"
```

**关键配套**：

- 提示词模板版本化管理（可回滚）
- 输出结构约束（JSON Schema 校验）
- 缓存：同条件同结果可短期复用，节省 API 成本

### 4.7 为什么不需要 RAG/向量数据库？

| 方案                      | 数据量要求  | 你们的情况           | 结论        |
| ----------------------- | ------ | --------------- | --------- |
| RAG                     | 万级以上   | 校园餐厅仅100-200条   | ❌ 不需要     |
| 向量数据库                   | 语义检索场景 | 数据量小，不需要额外检索系统  | ❌ 不需要     |
| **候选集 Prompt（固定 20 条）** | 任意规模   | 先规则/打分收敛再交给 LLM | ✅ **用这个** |

**正式版决策（统一口径）**：线上默认只给 LLM 输入**固定 20 条候选**，不走“全量餐厅列表直接塞 Prompt”。

**理由**：

- **可控延迟与成本**：LLM 是最大耗时点，固定候选规模能把 P95 延迟和 token 成本锁死
- **更稳定的输出质量**：候选过多会让模型“分心”，更容易出现遗漏与理由泛化
- **可扩展性**：今天是 100-200 条，明天数据源扩展后不必推倒重来

> 说明：数据量小的价值在于“不需要检索系统”，而不是“必须全量喂给 LLM”。全量输入仅用于离线评测或调试对比，不作为在线主路径。

***

## 4.8 pnpm Monorepo 工程结构

```
mealmuse/                              # 项目根目录
├── package.json                      # 根 package.json（pnpm workspaces 配置）
├── pnpm-workspace.yaml               # pnpm 工作区配置
├── tsconfig.json                     # 根 TS 配置（引用路径映射）
├── turbo.json                        # Turborepo 任务编排
├── .env.example                      # 环境变量示例（不包含任何敏感值，可提交）
│
├── packages/
│   └── shared/                       # 前后端共享包
│       ├── package.json              # name: "@mealmuse/shared"
│       ├── src/
│       │   ├── types/                # 共享 TypeScript 类型
│       │   │   ├── restaurant.ts     # Restaurant, Cuisine, SpicyLevel 等
│       │   │   ├── recommend.ts      # RecommendResult, SwitchDirection 等
│       │   │   ├── room.ts           # Room, Member, GroupResult 等
│       │   │   └── session.ts        # SessionHistory, UserFeedback 等
│       │   ├── constants/            # 共享枚举和常量
│       │   │   ├── scene.ts          # Scene 枚举（赶时间/省钱/吃爽...）
│       │   │   ├── cuisine.ts        # Cuisine 枚举（米饭/面食/小吃...）
│       │   │   └── constraints.ts    # SpicyLevel, BudgetRange 等
│       │   └── prompts/              # Prompt 模板
│       │       ├── single-recommend.ts
│       │       ├── group-recommend.ts
│       │       └── system-prompt.ts
│       └── tsconfig.json
│
├── apps/
│   ├── client/                       # Taro 前端
│   │   ├── package.json              # name: "@mealmuse/client"
│   │   ├── tsconfig.json             # paths: { "@shared/*": ["../../packages/shared/src/*"] }
│   │   ├── config/                   # Taro 配置
│   │   │   ├── index.ts
│   │   │   ├── dev.ts
│   │   │   └── prod.ts
│   │   ├── src/
│   │   │   ├── app.tsx
│   │   │   ├── pages/                # 页面
│   │   │   ├── components/           # 组件
│   │   │   ├── stores/               # Zustand stores
│   │   │   ├── services/             # API 请求封装
│   │   │   └── utils/
│   │   └── project.config.json       # 微信小程序配置
│   │
│   └── server/                       # NestJS 后端
│       ├── package.json              # name: "@mealmuse/server"
│       ├── tsconfig.json             # paths: { "@shared/*": ["../../packages/shared/src/*"] }
│       ├── nest-cli.json
│       ├── src/
│       │   ├── main.ts               # 入口
│       │   ├── app.module.ts         # 根模块
│       │   ├── meal/                 # 餐食库模块
│       │   ├── recommend/            # 推荐引擎模块
│       │   ├── llm/                  # 大模型模块
│       │   ├── room/                 # 多人房间模块
│       │   ├── session/              # 会话历史模块
│       │   └── profile/              # 偏好画像模块
│       └── test/
│
└── data/
    └── mock-restaurants.json         # Mock 餐厅数据
```

**根 package.json（pnpm workspaces 配置）**：

```json
{
  "name": "mealmuse",
  "private": true,
  "scripts": {
    "dev:client": "pnpm --filter @mealmuse/client dev:h5",
    "dev:server": "pnpm --filter @mealmuse/server start:dev",
    "dev:weapp": "pnpm --filter @mealmuse/client dev:weapp",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
```

**pnpm-workspace.yaml**：

```yaml
packages:
  - 'packages/*'
  - 'apps/*'
```

**类型共享示例**：

```typescript
// packages/shared/src/types/recommend.ts
export interface RecommendResult {
  oneLiner: string;
  main: RecommendItem;
  alternatives: RecommendItem[];
  anti: AntiRecommend;
  switchHints: Record<SwitchDirection, string>;
}

// apps/server/src/recommend/recommend.service.ts
import { RecommendResult } from '@shared/types/recommend';

// apps/client/src/pages/result/index.tsx
import type { RecommendResult } from '@shared/types/recommend';
```

**Monorepo 的核心价值**：

- 前后端共享 Restaurant、RecommendResult 等类型定义，改一处两端同步
- 共享 Scene、Cuisine、SpicyLevel 等枚举常量，避免前后端硬编码不一致
- Prompt 模板集中管理在 shared 包，后端引用生成，前端不需要关心

### 4.9 前端页面结构与路由

```
pages/
├── index/                  # 首页（入口：单人/群体选择）
├── single/
│   ├── filter/             # 单人3问筛选页
│   └── result/             # 单人推荐结果页
├── group/
│   ├── create/             # 创建/加入房间页
│   ├── waiting/            # 等待成员填写页
│   └── result/             # 群体推荐结果页
└── history/                # 历史记录页

components/                  # 公共业务组件
├── RecommendCard/          # 推荐卡片（主推/备选复用）
├── AntiRecommendCard/      # 反推荐卡片
├── FilterQuestion/         # 筛选问题卡片（3问复用）
├── SwitchButton/           # "换一个"方向按钮组
├── SkeletonResult/         # 推荐结果骨架屏
└── MemberAvatar/           # 群体成员头像列表
```

**路由配置**（app.config.ts）：

```typescript
pages: [
  'pages/index/index',           // 首页
  'pages/single/filter/index',   // 单人筛选
  'pages/single/result/index',   // 单人结果
  'pages/group/create/index',    // 创建房间
  'pages/group/waiting/index',   // 等待成员
  'pages/group/result/index',    // 群体结果
  'pages/history/index',         // 历史记录
]
```

**Zustand Store 划分**：

| Store               | 管理内容                      |
| ------------------- | ------------------------- |
| `useFilterStore`    | 单人筛选3问的选中状态               |
| `useRecommendStore` | 推荐结果、加载状态、流式文本            |
| `useRoomStore`      | 房间信息、成员列表、WebSocket 连接状态  |
| `useProfileStore`   | 用户偏好画像（从 localStorage 读取） |

***

## 五、"换一个"方向性刷新

用户点击"换一个"时，本质是对同一候选池做**带方向性的二次重排**：

| 换一个方向        | 权重调整策略                | Prompt 指令         |
| ------------ | --------------------- | ----------------- |
| **换个更便宜的**   | 预算权重提升至50%，对高价项额外惩罚   | "优先推荐价格更低的选项"     |
| **换个更快的**    | 时间权重提升至50%，对排队久项惩罚    | "优先推荐出餐最快的选项"     |
| **换个更健康的**   | 健康权重提升至50%，对油/糖/重口项惩罚 | "优先推荐轻油少盐的选项"     |
| **换个更爽的**    | 口味权重提升至50%，重口味加分      | "优先推荐口味更丰富的选项"    |
| **换个更近的**    | 距离权重提升至50%（若有定位数据）    | "优先推荐距离最近的选项"     |
| **换个更有饱腹感的** | 饱腹感权重提升至50%           | "优先推荐分量足、有饱腹感的选项" |

**实现方式**：在原有 Prompt 基础上，追加方向指令并调整权重规则，重新调用 LLM。

***

## 六、多人房间实时同步架构

### 6.1 房间形态

| 版本      | 实现方式                    | 优先级 |
| ------- | ----------------------- | --- |
| **最小版** | 同一设备多人依次填写，不需要后端房间      | P0  |
| **增强版** | 生成房间码/链接，Socket.IO 实时同步 | P1  |

### 6.2 Socket.IO Room 架构

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   用户 A    │     │   用户 B    │     │   用户 C    │
│  (创建者)   │     │  (扫码加入) │     │  (扫码加入) │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │ WebSocket
                           ▼
              ┌─────────────────────────┐
              │    Socket.IO Server     │
              │  ┌─────────────────┐    │
              │  │   Room: abc123  │    │
              │  │  ├─ 用户 A      │    │
              │  │  ├─ 用户 B      │    │
              │  │  └─ 用户 C      │    │
              │  └─────────────────┘    │
              └─────────────────────────┘
```

### 6.3 房间生命周期

```
1. 创建房间 (HTTP POST /api/rooms)
   └─ 返回 roomId: "abc123"

2. 分享房间 (生成二维码/链接)
   └─ 链接: https://mealmuse.com/room/abc123

3. 加入房间 (WebSocket: join-room)
   └─ socket.join("abc123")

4. 填写偏好 (WebSocket: update-preference)
   └─ io.to("abc123").emit("member-updated", data)

5. 发起推荐 (HTTP POST /api/rooms/:id/recommend)
   └─ 调用 LLM → 生成结果
   └─ io.to("abc123").emit("result-ready", result)

6. 关闭房间 (WebSocket 断开 / 超时24小时自动关闭)
```

### 6.4 群体推荐策略（可解释、可控）

```
对每个候选餐食，分别计算对每个成员的满意度（0-100）
→ 剔除硬冲突（任何成员忌口命中 → 直接排除）
→ 计算群体分数：
    群体分 = 平均满意度 - 最低满意度惩罚 - 冲突惩罚
→ 选择群体分最高的作为"最终推荐"
→ 输出：综合匹配度 + 每人满意度 + 妥协点解释（由大模型生成）
```

**群体推荐 Prompt 示例**：

```
【System】
你是校园群体吃饭决策助手。请根据多位成员的偏好，找到群体最优折中方案。
推荐结果要让每个人都"能接受"，并解释主要的妥协点。

【成员偏好】
- 用户A（小明）：预算20元，想吃米饭，不吃辣，赶时间
- 用户B（小红）：预算30元，想吃面食，能吃辣，都行
- 用户C（小刚）：预算15元，随便吃，不吃香菜，想省钱

【群体评分规则】
群体分 = 平均满意度 - 最低满意度惩罚(低于60时每低1分扣2分) - 忌口冲突惩罚(每触发一次扣20分)

【候选餐厅】
1. 二食堂自选餐 - 12-22元 - 15分钟 - 标签：多样、非辣微辣都有
2. 砂锅米线 - 16元 - 20分钟 - 标签：热汤、含香菜
...（共20条）

请输出：
{
  "oneLiner": "一句话群体推荐",
  "main": { "name": "", "groupScore": 84, "reasons": [] },
  "memberSatisfaction": [
    { "name": "小明", "score": 78 },
    { "name": "小红", "score": 88 },
    { "name": "小刚", "score": 86 }
  ],
  "compromises": ["小明需要接受非米饭选项", "预算取交集12-20元"],
  "conflicts": ["砂锅米线因小刚不吃香菜被排除"]
}
```

***

## 七、会话历史记录与偏好记忆

### 7.1 P0：localStorage（先做）

**存储内容**：

- 会话输入：三问答案（场景/类型/限制）
- 会话输出：主/备/反（含餐食 ID、分数、关键字段）
- 时间：createdAt
- 行为：用户点了"就吃这个/换一个/不喜欢"等

**存储周期**：保留最近 50-100 条，避免无限增长。

### 7.2 P1：同步到数据库

- 前端仍先写 localStorage（体验不卡）
- 后端提供"批量上报"接口异步同步
- 后端进度允许时再接入

### 7.3 偏好画像（轻量加权）

从历史会话中统计，用于影响下次推荐：

| 统计项           | 存储内容             | 使用方式        |
| ------------- | ---------------- | ----------- |
| 常选 category   | \["米饭", "热汤"]    | 常选类型 +5 分   |
| 常选 scene      | \["赶时间"]         | 作为默认场景      |
| 常选 spicyLevel | 1 (微辣)           | 作为默认辣度      |
| rejectedTags  | \["香菜", "太油"]    | 直接排除或大扣分    |
| recentMeals   | \[id1, id3, id8] | 最近吃过同类 -5 分 |
| avgBudget     | 18               | 作为默认预算值     |

***

## 八、数据模型设计

### 8.1 餐厅数据 (Restaurant)

```javascript
{
  _id: ObjectId,
  name: "番茄牛腩饭",           // 餐厅名称
  location: "二食堂二楼",       // 位置
  cuisine: ["米饭", "热汤"],    // 类型标签
  price: 18,                    // 价格（元）
  waitTime: 22,                 // 预计等待时间（分钟）
  spicyLevel: 0,                // 辣度 0-3（0不辣 3越辣越好）
  tags: ["热乎", "下饭", "赶课友好"],
  rejectedTags: ["香菜"],       // 包含的忌口标签（用于硬排除）
  healthScore: 3,               // 健康评分 1-5
  fillingScore: 4,              // 饱腹感评分 1-5
  image: "https://...",         // 图片URL
  rating: 4.5,                  // 评分
  createdAt: Date,
  updatedAt: Date
}
```

### 8.2 用户偏好 (UserPreference)

```javascript
{
  _id: ObjectId,
  userId: "anonymous_001",      // 匿名用户ID（设备标识）
  favoriteCuisines: ["米饭", "热汤"],
  dislikedTags: ["香菜", "太油"],
  avgBudget: 18,
  preferredSpicyLevel: 1,
  recentMeals: [
    { restaurantId: "...", timestamp: Date }
  ],
  createdAt: Date,
  updatedAt: Date
}
```

### 8.3 历史会话 (SessionHistory)

```javascript
{
  _id: ObjectId,
  userId: "anonymous_001",
  type: "single" | "group",
  roomId: "abc123",             // 群体房间ID（单人则为空）
  
  // 输入
  preferences: {
    scene: "赶时间",
    budget: 20,
    cuisine: ["米饭"],
    constraints: { spicy: 0, taboos: ["香菜"], timeLimit: 30 }
  },
  
  // 输出
  result: {
    main: { name: "番茄牛腩饭", matchScore: 87 },
    alternatives: [{}, {}],
    anti: {}
  },
  
  // 反馈
  feedback: {
    accepted: true,
    changedTo: null,            // 换成了什么
    switchDirection: null       // 换的方向（cheap/fast/healthy...）
  },
  
  createdAt: Date
}
```

### 8.4 群体房间 (Room)

```javascript
{
  _id: ObjectId,
  roomId: "abc123",             // 短ID，用于分享
  creatorId: "anonymous_001",
  memberCount: 3,
  members: [
    {
      userId: "anonymous_001",
      nickname: "小明",
      preferences: { budget: 20, cuisine: ["米饭"], spicy: 0, taboos: ["香菜"] },
      joinedAt: Date
    }
  ],
  status: "waiting" | "completed" | "closed",
  result: {},
  createdAt: Date,
  expiresAt: Date               // 24小时后自动过期
}
```

***

## 九、API 接口设计

### 9.1 单人推荐

```
POST /api/recommend/single
Request:
{
  "scene": "赶时间",
  "budget": 20,
  "cuisine": ["米饭"],
  "constraints": {
    "timeLimit": 30,
    "spicyLevel": 0,
    "taboos": ["香菜"]
  },
  "userId": "anonymous_001",
  "switchDirection": null        // 换一个方向：cheap/fast/healthy/tasty/near/filling
}

Response:
{
  "oneLiner": "今天推荐你吃番茄牛腩饭，热乎又快！",
  "main": {
    "id": "...", "name": "番茄牛腩饭", "matchScore": 87,
    "price": 18, "waitTime": 22, "location": "二食堂二楼",
    "reasons": ["在你的预算内", "出餐速度快，适合赶时间", "热乎有饱腹感"],
    "downside": "今天可能排队稍长"
  },
  "alternatives": [{ ... }, { ... }],
  "anti": {
    "name": "麻辣香锅",
    "reasons": ["等待时间长", "可能超预算", "不适合快速解决午饭"]
  },
  "switchHints": {
    "cheap": "鸡腿饭", "fast": "鸡腿饭", "healthy": "砂锅米线",
    "tasty": "麻辣香锅", "near": "一食堂盖浇饭", "filling": "自选餐"
  }
}
```

### 9.2 群体房间

```
POST /api/rooms                          创建房间
GET  /api/rooms/:roomId                  获取房间信息
POST /api/rooms/:roomId/members          提交成员偏好（HTTP 备选）
POST /api/rooms/:roomId/recommend        发起群体推荐

WebSocket Events:
  join-room          → 加入房间
  update-preference  → 提交个人偏好（实时）
  member-updated     → 广播成员更新
  result-ready       → 广播推荐结果
```

### 9.3 会话与历史

```
GET  /api/history?userId=xxx&page=1&limit=10    获取历史
POST /api/sessions/batch                          批量上报会话（P1）
POST /api/history/:sessionId/feedback             提交反馈
GET  /api/profile/:userId                         获取偏好画像
```

***

## 十、测试方案

### 10.1 测试分层策略

| 层级          | 工具                    | 覆盖范围       | 优先级 | AI 辅助方式             |
| ----------- | --------------------- | ---------- | --- | ------------------- |
| **E2E 端到端** | Python + Playwright   | 单人/多人完整流程  | P0  | AI 生成测试代码，人工 Review |
| **后端 API**  | Jest + Supertest      | 推荐/房间/历史接口 | P0  | AI 生成测试代码，Mock LLM  |
| **前端组件**    | React Testing Library | 筛选卡片/推荐卡片  | P1  | AI 生成组件测试           |
| **LLM 质量**  | AI 评估 AI              | 推荐理由合理性    | P1  | 批量评分，人工抽检           |

### 10.2 E2E 测试（Python Playwright）

**核心场景**（3 个）：

- 单人推荐：首页 → 筛选 3 问 → 结果页 → 验证主/备/反推荐
- 多人房间：创建房间 → 模拟成员加入 → 群体推荐 → 验证满意度展示
- 换一个功能：结果页点击换推荐 → 验证内容变化

**AI 生成测试代码流程**：

```
用户故事（人工写）
    ↓
AI 生成 Playwright 代码（Python）
    ↓
保存到 tests/e2e/，人工 Review
    ↓
pytest 执行，生成报告
```

### 10.3 后端 API 测试

**关键测试点**：

- 推荐接口：Mock LLM 响应，验证返回结构
- 降级策略：LLM 超时/失败时返回模板化结果
- 房间接口：CRUD + WebSocket 事件验证

### 10.4 AI 能力评估（定量）

| 指标        | 计算方法                       | 目标   |
| --------- | -------------------------- | ---- |
| **生成可用率** | AI 生成的测试代码中，直接可用或改 <5 行的比例 | >80% |
| **回归发现率** | AI 测试发现的 Bug / 总发现 Bug     | >60% |

***

## 十一、性能优化策略

### 11.1 性能瓶颈分析

本项目有 4 大性能瓶颈，按严重程度排序：

| 排名 | 瓶颈              | 严重程度 | 原因                      |
| -- | --------------- | ---- | ----------------------- |
| 1  | **LLM API 响应慢** | 🔴 高 | 用户等推荐会白屏/转圈 3-10 秒      |
| 2  | **Taro 跨端渲染性能** | 🟡 中 | 小程序/H5 列表滚动卡顿、包体积大      |
| 3  | **推荐编排计算耗时**    | 🟡 中 | 多人场景规则/打分/重排链路增加 CPU 消耗 |
| 4  | **数据库频繁读写**     | 🟢 低 | 会话历史、偏好画像读写拖慢接口         |

### 11.2 LLM 慢的解决方案（P0 必做）

这是最大的性能瓶颈。用户点"推荐"后，如果白屏等 5-10 秒，体验极差。

**方案：后端缓存 + 前端骨架屏 +（可选）流式输出，三管齐下**

```
用户点击"推荐"
    │
    ├─ 前端：立即展示骨架屏（不白屏）
    │
    ├─ 后端：先查缓存
    │   ├─ 命中 → 直接返回（<100ms）
    │   └─ 未命中 → 调用 LLM
    │       ├─ 可选：通过 Socket.IO 流式下发文案（前端逐字显示）
    │       └─ 结果写入缓存（下次命中）
    │
    └─ 前端：收到 Socket.IO token 事件，逐步渲染推荐理由

**小程序端说明（正式版口径）**：
- 小程序网络环境对流式能力支持不一致，不把 chunked/SSE 作为主路径
- 正式版默认：推荐结果一次性返回；如需要“逐字展示”，统一走 Socket.IO（WebSocket）事件
```

**缓存策略（三级缓存）**：

| 级别     | 方式              | 速度       | 持久性  | 适用数据      | 优先级 |
| ------ | --------------- | -------- | ---- | --------- | --- |
| **L1** | Node.js 内存缓存    | 最快（<1ms） | 重启丢失 | 餐食列表、场景权重 | P0  |
| **L2** | Redis 缓存        | 极快（<5ms） | 重启不丢 | 推荐结果、AI文案 | P1  |
| **L3** | 前端 localStorage | 本地（0ms）  | 永久   | 历史会话、用户偏好 | P0  |

**P0 阶段只用 L1（内存缓存），不需要 Redis**：

```typescript
// NestJS 中用 Map 实现简单内存缓存
@Injectable()
export class CacheService {
  private cache = new Map<string, { data: any; expires: number }>();

  get(key: string) {
    const item = this.cache.get(key);
    if (!item) return null;
    if (Date.now() > item.expires) {
      this.cache.delete(key);
      return null;
    }
    return item.data;
  }

  set(key: string, data: any, ttlSeconds: number = 3600) {
    this.cache.set(key, { data, expires: Date.now() + ttlSeconds * 1000 });
  }
}
```

**最该缓存的接口**：

| 接口    | 缓存 key                                    | 缓存时间 | 理由     |
| ----- | ----------------------------------------- | ---- | ------ |
| 单人推荐  | `rec:{scene}:{budget}:{cuisine}:{taboos}` | 24小时 | 同条件同结果 |
| 多人推荐  | `grp:{roomId}`                            | 1小时  | 房间内不变  |
| 餐食列表  | `meals:all`                               | 6小时  | 数据几乎不变 |
| AI 文案 | `ai:{hash(prompt+data)}`                  | 24小时 | 同输入同输出 |

### 11.3 Taro 跨端性能优化

**打包体积优化**：

- 关掉未使用组件，按需引入 Taro UI
- 图片全部用 CDN + WebP 压缩，不本地塞大图
- 路由懒加载，不在首屏加载所有页面

**列表性能（最卡的元凶）**：

- 长列表必须用虚拟列表（Taro 自带 `VirtualList` 组件）
- 不要一次性渲染几十上百条数据，分页加载
- 列表项少写嵌套标签，少用复杂样式

**请求性能**：

- 接口合并请求，别一个页面发 5-6 个独立请求
- 热门数据做本地缓存（localStorage/内存）
- 搜索框、下拉刷新必加节流/防抖

**渲染优化**：

- 页面不用的组件条件销毁（`if` 而非 `show`），别隐藏留着占资源
- 少用频繁 `setData`，合并状态更新
- 避免不必要的页面重渲染

**H5 不用 SSR 也能接近 SSR 体验**：

- 骨架屏：页面没加载完先出骨架屏（P0 必做）
- 静态资源 CDN + 压缩 + 懒加载
- 首屏关键数据预请求

### 11.4 关于 SSR/SSG

**明确放弃 SSR/SSG**，理由：

- 产品核心是小程序 + 移动端 App + 内部 H5
- 不靠百度 SEO，不靠搜索引擎引流
- 用户都是主动进入小程序/App，不是网页搜索进来
- Taro 天生不支持 SSR/SSG，不用硬凑
- **只要选了 Taro，就别纠结 SSR/SSG**

***

## 十二、后端模块与职责边界（正式版）

### 12.1 模块边界定义

本项目不引入“自治式 AI Agent”概念，后端只保留**可测试、可替换、职责明确**的业务模块/服务。推荐链路按“编排 + 可插拔步骤”组织。

```
用户请求
    │
    ▼
┌────────────────────────────────────────────────────────┐
│  recommend/RecommendService（编排器）                    │
│  └─ 接收请求，按顺序调度以下步骤                         │
├────────────────────────────────────────────────────────┤
│  1) rule-filter/RuleFilterService（硬约束过滤）          │
│     └─ 忌口/辣度/预算/时间等硬排除                        │
├────────────────────────────────────────────────────────┤
│  2) scoring/ScoringService（场景化打分）                 │
│     └─ 根据“最在意什么”切换权重，得到可解释的分解          │
├────────────────────────────────────────────────────────┤
│  3) rerank/RerankService（重排序）                       │
│     └─ 多样性约束、历史偏好加权、最近吃过惩罚              │
├────────────────────────────────────────────────────────┤
│  4) llm/LlmService（模型调用 + 输出校验 + 降级）          │
│     └─ 输入固定 20 条候选，输出统一 JSON 结构              │
├────────────────────────────────────────────────────────┤
│  5) cache/CacheService（缓存）                           │
│     └─ 查/写缓存、TTL、命中统计                           │
├────────────────────────────────────────────────────────┤
│  6) profile/ProfileService（画像）                        │
│     └─ 读取/更新轻量画像；影响重排默认值                   │
└────────────────────────────────────────────────────────┘
    │
    ▼
返回推荐结果
```

### 12.2 拆分原则（保持可维护性）

| 原则       | 说明                           |
| -------- | ---------------------------- |
| **单一职责** | 每个 Service 只做一件事，便于独立测试与替换   |
| **故障隔离** | LLM 超时/失败不影响主链路可用性，降级策略独立    |
| **可缓存**  | 推荐结果与 LLM 文案可缓存；缓存键必须可解释、可追踪 |
| **可替换**  | LLM 供应商/模型可切换，不影响上层编排与契约     |

### 12.3 服务间契约（结构化数据）

模块之间通过**结构化数据**通信，不通过自然语言：

```typescript
// RuleFilterService 输出 = ScoringService 输入
interface FilteredResult {
  candidates: Restaurant[];        // 过滤后的候选池
  excluded: { id: string; reason: string }[];  // 被排除的项及原因
}

// ScoringService 输出 = RerankService 输入
interface ScoredResult {
  candidates: (Restaurant & { score: number; scoreBreakdown: ScoreBreakdown })[];
}

// RerankService 输出 = LlmService 输入（最终收敛为 20 条）
interface RankedResult {
  main: Restaurant & { score: number };
  alternatives: (Restaurant & { score: number })[];
  anti: Restaurant & { antiReasons: string[] };
}
```

### 12.4 LLM 超时与熔断

防止大模型卡死整个后端服务：

```
LLM 调用
    │
    ├─ 设置超时：10 秒（可配置）
    │   ├─ 超时 → 降级到模板化理由
    │   └─ 返回缓存结果（如有）
    │
    ├─ 设置重试：最多 1 次
    │   └─ 重试仍失败 → 降级
    │
    └─ 设置熔断：连续 5 次失败 → 暂停 LLM 调用 60 秒
        └─ 60 秒内所有推荐走纯规则 + 模板理由
```

***

## 十三、数据库优化策略

### 13.1 餐食数据：常驻内存

餐食数据量小（100-200 条），几乎不变，**不需要每次查库**：

```typescript
// NestJS 中在 Service 初始化时加载到内存
@Injectable()
export class MealService {
  private meals: Restaurant[] = [];

  async onModuleInit() {
    this.meals = await this.mealModel.find().lean();
  }

  findAll(): Restaurant[] {
    return this.meals;  // 直接从内存返回，0 数据库查询
  }
}
```

### 13.2 索引设计

| 集合             | 索引字段                           | 用途           |
| -------------- | ------------------------------ | ------------ |
| SessionHistory | `{ userId: 1, createdAt: -1 }` | 按用户查历史，按时间倒序 |
| Room           | `{ roomId: 1 }`                | 房间码查询        |
| Room           | `{ status: 1, expiresAt: 1 }`  | 过期房间清理       |
| UserPreference | `{ userId: 1 }`                | 用户偏好查询       |

### 13.3 数据库设计原则

| 原则           | 说明                           |
| ------------ | ---------------------------- |
| **只存必要字段**   | 不存冗余大文本（推荐理由等可从 Prompt 重新生成） |
| **列表接口必须分页** | 绝不一次性查全表，`limit + skip`      |
| **会话数据定期清理** | 超过 30 天的会话记录归档或删除            |
| **房间数据自动过期** | 24 小时后自动关闭，定时任务清理            |

### 13.4 关于 Redis

**P0 阶段不需要 Redis**，用 Node.js 内存缓存（Map）即可。

**P1 阶段如果需要 Redis**（多人并发、多实例部署），接入方式：

```typescript
// NestJS 中用 @nestjs/cache-manager + cache-manager-redis-store
// 5 行代码即可接入，不需要额外学习成本
```

Redis 适用于以下场景才需要引入：

- 多个后端实例共享缓存
- 需要持久化缓存（重启不丢）
- 需要高级数据结构（Sorted Set 排行榜等）

***

## 十四、部署架构

### 14.1 开发环境

```
本地开发
├── Taro 前端 (pnpm dev:client / pnpm dev:weapp)
├── NestJS 后端 (pnpm dev:server)
└── MongoDB (本地或 Atlas 免费集群)
```

### 14.2 生产部署（MVP）

```
生产环境
├── 前端
│   ├── H5: Vercel / Netlify (免费)
│   └── 微信小程序: 微信开发者工具上传
├── 后端
│   └── Railway / Render / Fly.io (免费额度)
└── 数据库
    └── MongoDB Atlas (免费 512MB)
```

### 14.3 环境变量

```bash
PORT=3000
MONGODB_URI=mongodb+srv://...

# LLM 配置（切换模型只需改这三个值）
LLM_API_KEY=YOUR_LLM_API_KEY
LLM_BASE_URL=https://api.deepseek.com    # 或通义/OpenAI的URL
LLM_MODEL=deepseek-chat                   # 或 qwen-turbo / gpt-4o
```

**正式版要求**：

- 生产环境密钥只通过部署平台的 Secret/环境变量注入（Render/Railway/Fly.io 等）
- 仓库内仅允许存在 `.env.example`（无敏感值），禁止根目录 `.env` 承载敏感信息

***

## 十五、技术风险与应对

| 风险                  | 影响   | 应对策略                            |
| ------------------- | ---- | ------------------------------- |
| Taro-RN 编译失败        | 中    | 降级为 H5+小程序双端，RN 作为 P1           |
| Socket.IO 房间同步延迟    | 中    | P0 先做同设备多人，P1 再上实时同步            |
| LLM API 响应慢/失败      | 🔴 高 | 内存缓存 + 骨架屏 + 流式输出 + 模板降级 + 熔断机制 |
| LLM 输出格式不合规         | 中    | JSON Schema 校验 + 重试 1 次 + 降级    |
| MongoDB Atlas 连接数限制 | 低    | MVP 用户少，免费层足够                   |
| 前端包体积过大             | 中    | 按需引入组件 + 图片 CDN + 路由懒加载         |
| 列表滚动卡顿              | 中    | 虚拟列表 + 分页加载 + 减少嵌套标签            |

### 15.1 错误处理方案

**后端统一异常处理**（NestJS ExceptionFilter）：

| 错误类型     | HTTP 状态码 | 处理方式                       |
| -------- | -------- | -------------------------- |
| 参数校验失败   | 400      | NestJS ValidationPipe 自动拦截 |
| 业务逻辑错误   | 422      | 自定义 BusinessException      |
| LLM 调用失败 | 503      | 降级到模板化理由，不暴露给用户            |
| 数据库错误    | 500      | 记录日志，返回通用错误信息              |
| 房间不存在/过期 | 404      | 返回友好提示                     |

**统一响应格式**：

```typescript
// 成功
{ "code": 0, "data": {...}, "message": "success" }

// 失败
{ "code": 40001, "data": null, "message": "参数校验失败：预算不能为负数" }
```

**前端错误边界**：React ErrorBoundary 包裹页面，防止白屏。

### 15.2 日志方案

| 日志类型      | 工具                     | 记录内容                                   |
| --------- | ---------------------- | -------------------------------------- |
| HTTP 请求日志 | NestJS 内置 Logger       | 请求路径、方法、耗时、状态码                         |
| LLM 调用日志  | 自定义 Logger             | 输入摘要（哈希/长度）、模型、耗时、token 用量、是否命中缓存、是否降级 |
| 错误日志      | NestJS + console.error | 异常堆栈、上下文信息                             |
| 推荐追踪      | 可选（P2）                 | 每次推荐的过滤原因、得分分解                         |

**P0 阶段**：用 NestJS 内置 Logger + console，不引入额外日志框架。

**日志脱敏与 PII 留存（正式版要求）**：

- **不记录**：原始 Prompt 全文、用户原始输入原文、完整候选列表、任何密钥/Token/连接串
- **只记录摘要**：`promptHash`、候选数量、关键参数（scene/budget 区间化）、耗时、命中缓存、错误码
- **PII 识别范围**：昵称、精确位置、可反查的 userId/openId、聊天内容等
- **脱敏规则**：userId 仅保留前后 2-4 位，其余用 `*`；位置只保留食堂/区域级别；昵称做不可逆 hash
- **留存周期**：错误日志与调用元数据默认 7-14 天；会话历史按产品需求最小化留存（见 7 章），可随时清理

### 15.3 接口限流与安全

| 措施             | 方案                                             | 优先级 |
| -------------- | ---------------------------------------------- | --- |
| **LLM 接口限流**   | 同一 userId 每分钟最多 5 次推荐请求                        | P0  |
| **房间创建限流**     | 同一 IP 每小时最多创建 10 个房间                           | P0  |
| **CORS 配置**    | NestJS `app.enableCors()`，开发环境允许 `localhost:*` | P0  |
| **输入校验**       | NestJS DTO + class-validator，所有入参校验            | P0  |
| **API Key 保护** | LLM API Key 只存后端环境变量，永不暴露到前端                   | P0  |

### 15.4 代码规范

| 工具                      | 用途           | 说明                                                                            |
| ----------------------- | ------------ | ----------------------------------------------------------------------------- |
| **ESLint**              | 代码检查         | `@typescript-eslint/recommended` + React 插件，TS 和 JS 用法完全相同，只多一个 TS 解析器        |
| **Prettier**            | 格式化          | 统一缩进 2 空格、单引号、分号，不区分 JS/TS                                                    |
| **Husky + lint-staged** | commit 前自动检查 | `pre-commit` 阶段仅对暂存文件执行 lint/format，避免全量扫描拖慢开发                                |
| **commitlint**          | 提交信息校验       | `commit-msg` 阶段校验 Conventional Commits（`feat/fix/docs/refactor/chore/test` 等） |

**推荐约束（正式版口径）**：

- `pre-commit`：lint-staged（ESLint + Prettier）+（可选）敏感信息扫描
- `commit-msg`：commitlint（统一提交信息，便于回滚与发布说明）

**最小可用配置（参考）**：

```bash
pnpm add -D husky lint-staged @commitlint/cli @commitlint/config-conventional
pnpm husky install
```

```bash
npx husky add .husky/pre-commit "pnpm lint-staged"
npx husky add .husky/commit-msg "pnpm commitlint --edit $1"
```

```json
{
  "scripts": {
    "prepare": "husky install",
    "lint": "turbo run lint",
    "format": "prettier -w .",
    "commitlint": "commitlint"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{md,json,yml,yaml}": [
      "prettier --write"
    ]
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
```

> 注意：Husky/commitlint 只能拦截本地提交，团队协作仍建议在 CI 里跑 `lint/test` 做兜底。

### 15.5 敏感信息自动检测（Husky pre-commit）

**目的**：防止 API Key、数据库密码等敏感信息被意外提交到 Git 仓库。

**检测规则**：

| 检测项         | 匹配规则                         | 处理   |
| ----------- | ---------------------------- | ---- |
| 硬编码 API Key | `sk`+`-[a-zA-Z0-9]{20,}`     | 阻止提交 |
| 硬编码数据库连接串   | `mongodb\+srv://`、`mysql://` | 阻止提交 |
| 硬编码密码       | `password\s*[:=]\s*['"]`     | 阻止提交 |
| .env 文件误提交  | 暂存区包含 `.env` 文件              | 阻止提交 |

**实现方式**：

```
.husky/pre-commit          ← Husky 钩子入口
    │
    ├─ pnpm lint-staged       ← 仅对暂存文件执行 ESLint + Prettier
    └─ pnpm check-secrets     ← 敏感信息检测（可选）
```

**跨平台兼容**：检测脚本用 Node.js 编写（`scripts/check-secrets.js`），通过 npm script 调用，Windows 和 Mac 均可运行，不依赖 bash 版本。

### 15.6 Git 分支策略（1周 MVP）

```
main          ← 稳定版本，每天合并一次
├── dev       ← 日常开发分支
│   ├── feat/single-recommend   ← 单人推荐功能
│   ├── feat/group-room         ← 多人房间功能
│   └── feat/llm-integration    ← LLM 集成
```

**规则**：

- 功能分支从 `dev` 拉出，完成后 PR 合并回 `dev`
- 每天结束将 `dev` 合并到 `main`
- 命名规范：`feat/xxx`、`fix/xxx`、`docs/xxx`

***

## 十六、里程碑规划

### P0（1周 MVP / 黑客松 Demo）

```
Day 1: 项目初始化 + 数据库设计 + Mock数据
       ├─ pnpm monorepo 搭建（workspaces + shared 包）
       ├─ NestJS 后端项目搭建（@nestjs/cli）
       ├─ Taro 前端项目搭建（小程序为交付目标，H5 用于开发调试+演示）
       ├─ @nestjs/mongoose + MongoDB Atlas 连接
       ├─ 共享类型定义（Restaurant/RecommendResult/枚举）
       └─ 准备 20-50 条餐厅 Mock 数据

Day 2: 单人决策流程 (前端 + 后端)
       ├─ 单人筛选页 UI（3问卡片式）
       ├─ 硬约束过滤 API
       └─ 推荐结果页 UI（主推/备选/反推）

Day 3: LLM 集成 + 推荐算法
       ├─ LLM Service 模块（OpenAI SDK + 环境变量切换）
       ├─ Prompt 三段式设计（shared 包管理模板）
       ├─（可选）Socket.IO 流式下发 + 降级策略（llm-fallback.service）
       └─ 内存缓存服务（CacheService，推荐结果缓存 24h）

Day 4: 多人房间 + 实时同步
       ├─ @nestjs/websockets Gateway 搭建
       ├─ 群体偏好收集页
       └─ 群体推荐 Prompt 调试

Day 5: 历史记录 + 性能优化
       ├─ localStorage 会话存储
       ├─ 用户偏好画像统计
       ├─ 骨架屏（推荐结果页加载态）
       ├─ 餐食数据常驻内存（不每次查库）
       └─ 移动端适配

Day 6-7: 小程序适配 + Demo 准备
         ├─ 微信小程序编译测试
         ├─ Bug 修复
         └─ Demo 数据准备 + 演练
```

### P1（可上线试运行）

- Node 推荐服务 + MongoDB 会话/房间持久化
- 房间码多人协作（Socket.IO 实时同步）
- LLM 统一由后端调用，做缓存/降级
- 历史记录同步到数据库
- 用户偏好画像云端存储

### P2（体验增强）

- 地图距离/导航（高德地图集成）
- 运营后台维护餐食库
- 个性化默认值（基于偏好画像自动填充）
- 更丰富的"换一个"策略
- iOS 小组件 / 灵动岛扩展

***

## 附录 A：技术栈版本锁定

版本锁定以仓库内各 `package.json` / `pnpm-lock.yaml` 为准（避免文档与代码漂移）。本文档只保留“统一口径”与“引入建议”，防止出现“文档写 3.x，但仓库已是 4.x”的漂移问题。

### A.1 仓库已落盘（以当前仓库为准）

| 领域         | 组件               | 版本（口径）        | 依据                         |
| ---------- | ---------------- | ------------- | -------------------------- |
| 前端框架       | Taro             | 4.1.11        | `apps/client/package.json` |
| 前端框架       | React / ReactDOM | ^18.0.0       | `apps/client/package.json` |
| 后端框架       | NestJS           | ^11.0.1       | `apps/server/package.json` |
| TypeScript | TypeScript       | 前后端建议统一到同一主版本 | `apps/*/package.json`      |

### A.2 规划引入（按需求启用，启用后以 lockfile 为准）

| 领域      | 组件                 | 版本（建议口径）            | 说明                      |
| ------- | ------------------ | ------------------- | ----------------------- |
| 状态管理    | Zustand            | 与 React 18 兼容的最新稳定版 | 仅在需要全局状态时引入             |
| H5 UI   | antd-mobile        | 与 React 18 兼容的最新稳定版 | H5 端 UI 方案候选            |
| 小程序 UI  | taro-ui / NutUI    | 以 Taro 4 生态可用性为准    | 小程序端 UI 方案候选            |
| 数据库     | MongoDB + Mongoose | 8.x                 | 若走 MongoDB 路线           |
| 实时通信    | Socket.IO          | 4.x                 | 多人房间/流式下发通道             |
| LLM SDK | OpenAI 兼容 SDK      | 4.x                 | 统一 OpenAI-compatible 接口 |

## 附录 B：推荐结果统一结构（前后端契约）

```javascript
// 所有推荐接口返回统一结构，便于前端展示与后端存储
{
  oneLiner: "string",              // 一句话推荐
  main: {
    id: "string",
    name: "string",
    matchScore: number,            // 0-100
    price: number,
    waitTime: number,
    location: "string",
    reasons: ["string", "string", "string"],  // 3条推荐理由
    downside: "string"             // 1条小缺点
  },
  alternatives: [{ /* 同main结构 */ }, { /* 同main结构 */ }],
  anti: {
    name: "string",
    reasons: ["string", "string"]  // 反推荐理由
  },
  switchHints: {                   // "换一个"方向提示
    cheap: "string",
    fast: "string",
    healthy: "string",
    tasty: "string",
    near: "string",
    filling: "string"
  }
}
```

